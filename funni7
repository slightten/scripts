getgenv().Dungeon = "Desert Temple"
getgenv().Difficulty = "Easy"
getgenv().Hardcore = "False"

if Dungeon == "Desert Temple" and Difficulty == "Easy" and Hardcore == "False" then

    local args = {[1] = "Desert Temple",[2] = "Easy",[3] = 0,[4] = false,[5] = false,[6] = false}
    game:GetService("ReplicatedStorage").remotes.createLobby:InvokeServer(unpack(args))
    game:GetService("ReplicatedStorage").remotes.startDungeon:FireServer()   
    end
    
        
        
    
    if game.PlaceId == 2606294912 then
    
    game:GetService("ReplicatedStorage").remotes.changeStartValue:FireServer()
    
    if not pcall(function() return syn.protect_gui end) then
        syn = {}
        syn.protect_gui = function(egg)
            egg.Parent = game.CoreGui
        end
    end
    
    local Library = loadstring(game:HttpGet("https://pastebin.com/raw/edJT9EGX", true))()
    
    local Window = Library:CreateWindow"Auto Farm"
    
    Window:AddToggle({text = "Enemy Farm Dungeons", flag = "toggle", state = false, callback = function(z)
    ouw = z
    while ouw do wait()
    for i,v in pairs(game:GetService("Workspace").dungeon:GetDescendants()) do
        if v:IsA "Humanoid" then
            v.Health = 0
        end
    end
    end
    end})
    
    Window:AddToggle({text = "Enemy Farm Wave Defence", flag = "toggle", state = false, callback = function(z)
    ouw = z
    while ouw do wait()
    for i,v in pairs(game:GetService("Workspace").enemies:GetDescendants()) do
        if v:IsA "Humanoid" then
            v.Health = 0
        end
    end
    end 
    end})
    
    Window:AddLabel({text = "If Enemies Dont Instantly Die Then Get Close To Them"})
    Window:AddLabel({text = "Then Get Close To Them"})
    
    local Window = Library:CreateWindow"Misc"
    
    Window:AddButton({text = "Copy Discord", flag = "button", callback = function()
    setclipboard'https://discord.gg/amateurhub'
    end})
    
    Window:AddBind({text = "Toggle UI", key = "RightControl", callback = function() Library:Close()
    end})
    
    Library:Init()
    end
    end
